#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	int n, k;
	double h;
	cin >> n;

	double* a = new double[n];
	for (int i = 0; i < n; i++)
	{
		cout << i<< "       ";
		cin >> a[i];
		cout << endl;
	}
	cout << endl;

	for (int i = 0; i < n; i++)
	{

		cout << a[i] << "\t";

	}
	cout << endl;
	cin >> k;
	h = 0;
	double* b = new double[n];

	for (int i = 0; i <= k; i++)
	{
		h = h + a[i];
	}
	cout << endl;
	for (int i = 0; i <= (k-1); i++)
	{
		b[i] = a[i];

	}

	b[k] = (h / (k +1));

	for (int i = k+1; i < n; i++)
	{
		b[i] = a[i];

	}
	cout << endl;
	cout << endl;
	for (int i = 0; i < n; i++)
	{
		cout << b[i] << "\t";
	}

}
