#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	int n, k, u;
	cin >> n;
	
	int* a = new int[n];
	for (int i = 0; i < n; i++)
	{
		cout << i << "            ";
		cin >> a[i];
		cout << endl;
	}
	cout << endl;

	for (int i = 0; i < n; i++)
	{

		cout << a[i] << "\t";

	}
	cout << endl;
	u = 0;

	for (int i = 0; i < n; i++)
	{
		if (a[i] % 2 == 1)
		{
			u = a[i];
		}
	}
	cout << endl;

	for (int i = 0; i < n; i++)
	{
		if (a[i] % 2 == 1)
		{
			a[i] = a[i] + u;
		}
	}
	cout << endl;
	for (int i = 0; i < n; i++)
	{
		cout <<a[i] << "\t";
	}
}
