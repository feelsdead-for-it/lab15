#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	int n, k;
	double h;
	cin >> n;

	double* a = new double[n];
	for (int i = 0; i < n; i++)
	{
		cout << i << "         ";
		cin >> a[i];
		cout << endl;
	}
	cout <<endl;

	for (int i = 0; i < n; i++)
	{

	cout << a[i] << "\t";

	}

	cout << endl;

	h = 0;

	for (int j = 0; j < n - 1; j++)
	{
		for (int i = 0; i < n - j - 1 ; i++)
		{
		if (a[i] > a[i+1])
			{
			h = a[i];
			a[i] = a[i+1];
			a[i+1] = h;
			}
		}
	}
		cout << endl;
		for (int i = 0; i < n; i++)
		{
			cout << a[i] << "\t";
		}
	
}
